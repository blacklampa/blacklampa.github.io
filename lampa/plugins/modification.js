(function () {
  'use strict';

  // BlackLampa is initialized early from `lampa/app.min.js` via `lampa/blacklampa/bl.init.js`.
  // This plugin entrypoint is intentionally kept as a no-op placeholder (too late for PHASE 0).
})();
